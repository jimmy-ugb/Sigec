class Trip{
  final String title;
  final DateTime starDate;
  final DateTime endDate;
  final double budget;
  final String costo;

  Trip(this.title, this.starDate, this.endDate, this.budget, this.costo);
}